package com.texnedo;

import com.texnedo.utils.Point;

import java.util.Objects;

public class MaxPointsOnLine {
    public static void main(String[] args) {
        MaxPointsOnLine pointsOnLine = new MaxPointsOnLine();
        System.out.println(pointsOnLine.maxPoints(null));
    }

    public int maxPoints(Point[] points) {
//        if (points == null || points.length == 0) {
//            throw new IllegalArgumentException();
//        }
//        for (int i = 0; i < points.length; i++) {
//
//        }
        return 0;
    }

//    static double computeK(Point p1, Point p2) {
//        return (p2.y - p1.y) / (p2.x - p1.x);
//    }
//
//    static int computeB(Point p, )
//
//    static class Line {
//        double k;
//        double b;
//
//
//        @Override
//        public boolean equals(Object o) {
//            if (this == o) return true;
//            if (o == null || getClass() != o.getClass()) return false;
//            Line line = (Line) o;
//            return Double.compare(line.k, k) == 0 &&
//                    Double.compare(line.b, b) == 0;
//        }
//
//        @Override
//        public int hashCode() {
//            return Objects.hash(k, b);
//        }
//    }
}
